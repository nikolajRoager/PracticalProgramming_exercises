set term png
set out "pendulum.png"
set title "pendulum"
set xlabel "$t$"
set key below
plot  "data.tsv" using 1:2 with lines title "$\theta(t)$",\
"data.tsv" using 1:3 with lines title "$\omega(t)$"
